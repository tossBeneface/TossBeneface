name: Deploy to EC2

on:
  push:
    branches:
      - master  # master 브랜치에 push될 때 실행

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t jean202/toss-beneface .

      - name: Push Docker Image to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker push jean202/toss-beneface:latest

      - name: SSH into EC2 & Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            echo "${{ secrets.EC2_PRIVATE_KEY }}" | base64 --decode > /tmp/ec2-key.pem
            chmod 600 /tmp/ec2-key.pem
            ssh -i /tmp/ec2-key.pem ubuntu@${{ secrets.EC2_HOST }} << EOF
              cd /home/ubuntu
              ./update.sh
            EOF
